## Installation
MacOS
```shell
brew install zeromq
brew install czmq
brew install json-c
```

Ubuntu
```shell
sudo apt-get install libzmq3-dev
sudo apt-get install libczmq-dev
sudo apt-get install libjson-c-dev
```
